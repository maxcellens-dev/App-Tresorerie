// Écran plein écran d'édition des budgets par catégorie (?month=YYYY-MM pour cibler un mois).
// Même schéma que la création de projet : la logique vit dans le composant, la route ne fait que
// l'exposer.
export { default } from '../../../components/budget/BudgetEditScreen';
